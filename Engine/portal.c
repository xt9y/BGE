#include "portal.h"

void render_quad(level_quad_t *b, level_quad_t *a) 
{
    /* is called inside level_render if there are to quads withe the same portal id, these are then inputted into the portal_render() function instead of render_quad */ 
}
